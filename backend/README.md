```
npm install
npm run dev
```

```
open http://localhost:3000
```

Set `OPENROUTER_API_KEY` in `backend/.env` to route the proxy through OpenRouter.
If you want the free router by default, add `OPENROUTER_DEFAULT_MODEL=openrouter/free` too.
